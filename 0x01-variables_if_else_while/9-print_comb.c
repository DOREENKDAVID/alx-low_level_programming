#include <stdio.h>

/**
 * main - prints the alphabet in lowercase
 *
 * Return: Always (Success)
 */
int main(void)
{
int  n;

for (n = '0'; d <= '9'; n++)

{
putchar(n);

if (n != '9')
{
putchar(',');
putchar(' ');
}
}

putchar('\n');
return (0);
}
